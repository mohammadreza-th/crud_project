import React from "react";

const placeHolder = ({ children }) => {
  return (
    <div>
      <h1>your text will apear hear!</h1>
      <p>{children}</p>
    </div>
  );
};

export default placeHolder;
